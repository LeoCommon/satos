# Run gpsd as daemon
START_DAEMON="true"

# Do not use any devices by default, daemon should be started with valid devices only
DEVICES=""

# Dont wait for clients to connect
GPSD_OPTIONS="-n"

# Add new usb devices to the daemon automatically
USBAUTO="true"