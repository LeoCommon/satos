[all]
arm_64bit=1

# Enable uart for debugging
enable_uart=1
uart_2ndstage=1

# disable bluetooth, to get ttyAMA0 back
dtoverlay=disable-bt

# Disable splash screen
disable_splash=1

# How much memory in MB to assign to the GPU
gpu_mem=16

# enable the watchdog
dtparam=watchdog

[pi4]
# Enable cpu boost to 1.8Ghz on later models
arm_boost=1

[pi5]
# We dont use the new UART connector, so enable the old way
dtparam=uart0_console

[all]
# Boot u-boot first so we can run a/b slot
kernel=u-boot.bin